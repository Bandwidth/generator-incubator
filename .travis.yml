language: node_js
node_js:
  - "0.11"
  - "0.10"
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != "generator-incubator" ]; then cd .. && eval "mv $currentfolder generator-incubator" && cd generator-incubator; fi

