{
  "name": "<%= _(projectName).trim().slugify() %>",
  "version": "0.0.0",
  "description": "<%= _(projectDescription).trim() %>",
  "private": true,
  "repository": {
  	"type": "git",
  	"url": "<%= _(projectGitRepoUrl).trim() %>"
	},
	"scripts": {
		"test": "grunt"
	}
}
